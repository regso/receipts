import 'package:flutter/material.dart';

class CookingStepsWidget extends StatelessWidget {
  const CookingStepsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('cooking steps');
  }
}
